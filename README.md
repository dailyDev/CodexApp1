# Basic Three-Column File Reader

This repository contains a small Python app that reads a text file where each non-empty line has exactly three whitespace-separated columns.

## Run

```bash
python3 app.py test_data.txt
```

## Expected format

- Blank lines and lines starting with `#` are ignored.
- Every data line must have exactly three columns.
